<h1>Edit the <%= @category[:name] %> Category</h1>

<form class="inline" action="/budget/<%= @category[:id] %>/update" method="post">
  
    <label for="new_category_name">Category Name:</label>

    <div class="form-item">
    <input name="new_category_name" id="new_category_name" value="<%= @category[:name] %>">
    </div>

    <label for="new_amount">Amount to budget:</label>

    <div class="form-item">
    $<input type="number" min="0.00" max="100000.00" step="0.01" name="new_amount" id="new_amount" value="<%= @category[:amount] %>">
    </div>

    <button class="edit" type="submit">Save</button>
</form>

<form class="inline" action="/" method="get">
  <button class="delete" type="submit">Cancel</button>
</form>

<h2>Total money budgeted so far: <%= total_money_budgeted %> / Available to budget: <%= money_available_to_budget %></h2>